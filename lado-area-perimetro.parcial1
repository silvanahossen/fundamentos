#include <iostream>
#include <math.h>

using namespace std;

int main()
{
    float lado , area ,p;
    cout<<"ingrese el  lado: ";
    cin>>lado;
    
    area=lado*lado;
    
    p = lado + lado + lado + lado;
    
    cout<<"el area del cuadrado es:"<<area<<endl;
    cout<<"el perimetro del cuadrado es:"<<p<<endl;

    return 0;
}
